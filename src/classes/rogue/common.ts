import { ActionIcon } from "../../types/Action";
import { ClassColours } from "../common";
import iconUrl from "./icon.svg";

export const RogueIcon: ActionIcon = {
  url: iconUrl,
  colour: ClassColours.Rogue,
};
